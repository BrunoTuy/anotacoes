# Atalhos do git no zsh

Criar a pasta do projeto
`mkdir projeto`

Dentro da pasta criada executar esses comandos
```
git init
git config --bool core.bare true
```

Primeiro push remoto
```
git push --set-upstream origin master
```

###### #git
